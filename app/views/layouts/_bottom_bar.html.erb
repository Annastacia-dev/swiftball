<div class="fixed bottom-0 w-full bg-white dark:bg-black border-t border-gray-200 dark:border-gray-900 md:hidden px-2 py-2">
  <div class="flex justify-around py-3">
  <% bottom_nav_links.each do |link| %>
    <%= link_to link[:path], class: "text-center flex flex-col text-lg font-bold" do %>
        <i class="<%= link[:icon_class] %>" title="<%= link[:menu_text] %>"></i>
    <% end %>
  <% end %>
  </div>
</div>